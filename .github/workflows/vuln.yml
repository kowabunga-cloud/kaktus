name: Vulnerabilities Check

on:
  push:
    branches: master
    paths:
      - 'cmd/kaktus/**'
      - 'internal/kaktus/**'
  pull_request:
    branches: master
  workflow_dispatch:

jobs:
  vuln:
    uses: kowabunga-cloud/workflows/.github/workflows/go-vuln.yml@master
    secrets: inherit
    with:
      pkgs: "librados-dev librbd-dev gcc"
